Once run the ny_covid_19.py file in Python environment which will schedule to run every day at 9 AM. 
which is read data from the API and writing the data into SQL Server/CSV files using Multithread option.

